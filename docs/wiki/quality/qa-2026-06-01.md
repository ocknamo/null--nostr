# QA 2026-06-01 — iOS Rust FFI Phase 1.1 Read-only Diagnostics

## Summary

iOS Rust FFI Phase 1.1 read-only diagnostics were manually confirmed from the iOS Settings UI.

## Result

**PASS**

Observed UI text:

```text
MLS DB: 暗号化済み / グループ 0件 / 更新待ち 0件
```

## Checks

| Check | Result |
|---|---|
| Rust FFI diagnostic is inside expanded セキュリティ設定 | PASS |
| `mlsIsEncrypted() -> Bool?` reaches live FFI and returns encrypted | PASS |
| `mlsListGroups()` read-only count is displayed | PASS — 0 groups |
| `mlsGroupsNeedingSelfUpdate(thresholdSecs:)` read-only count is displayed | PASS — 0 pending |
| No signing / publishing / key generation added | PASS |
| No private key / pubkey / DB path displayed | PASS |

## Notes

This closes iOS Rust FFI Phase 1.1 only. Full iOS Rust FFI remains future work; signing, publishing, key generation, and broader Talk/MLS live-path QA remain out of scope for this phase.

## Source references

- `ios/NuruNuru/Data/NostrRepository.swift`
- `ios/NuruNuru/Views/Screens/SettingsView.swift`
- `ios/GUARDRAILS.md`
- `docs/wiki/architecture.md`

## Related pages

- [[architecture]]
- [[quality/qa-template]]
- [[platforms/ios]]
- [[platforms/rust-engine]]

## Follow-up: Phase 1.2 manual QA PASS

Phase 1.2 diagnostic polish was manually confirmed from the iOS Settings UI.

Observed behavior:

- The Rust FFI diagnostic remains inside expanded セキュリティ設定.
- The diagnostic shows sanitized read-only counts and a local check time.
- The refresh button reloads the diagnostic.
- No private key, pubkey, DB path, relay auth challenge, or raw Rust/UniFFI error is displayed.

Result: **PASS**
